{
  "name": "ra2311026010437",
  "version": "1.0.0",
  "private": true,
  "description": "Backend project (RA2311026010437)",
  "main": "dist/logging_middleware/index.js",
  "types": "dist/logging_middleware/index.d.ts",
  "scripts": {
    "build": "tsc",
    "railway:build": "npm run build && npm ci --prefix notification_app_be && npm run build --prefix notification_app_be && npm ci --prefix vehicle_maintence_scheduler && npm run build --prefix vehicle_maintence_scheduler",
    "start": "node scripts/start-all.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
